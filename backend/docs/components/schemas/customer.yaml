Customer:
  type: object
  required:
    - id
    - user
  properties:
    id:
      type: string
      description: Unique customer ID.
    userId:
      type: string
      description: Customer's user ID.
